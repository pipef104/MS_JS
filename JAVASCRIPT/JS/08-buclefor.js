'use strict';

//Bucle for
// Es una estructura de control que se repite varias veces

var numero = 100;

for (let index = 0; index <= numero; index++) {
  console.log("Vamos por el numero: " + index);
  if (index = 40) {
    console.log("llego a 40");
  }
  
}